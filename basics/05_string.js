const name = "jiten-vaid-cap-sweden"
const repoCount =50

console.log(name + repoCount + "value") // not used now

console.log(`Hello ${name} you have repo ${repoCount}`)

console.log(name.split('-')) // convert string into array
